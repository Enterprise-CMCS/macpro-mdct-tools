#!/usr/bin/expect -f

    spawn op account add --address $env(MDCT_OP_ADDRESS) --email $env(MDCT_OP_EMAIL) --secret-key $env(MDCT_OP_KEY)
    expect "Enter the password for mdct-integrations"
    send -- "$env(MDCT_OP_PASSWORD)\r"
    expect eof