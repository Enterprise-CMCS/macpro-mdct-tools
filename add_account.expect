#!/usr/bin/expect -f
set timeout -1

spawn op account add --address $env(MDCT_OP_ADDRESS) --email $env(MDCT_OP_EMAIL) --secret-key $env(MDCT_OP_KEY)
expect "Enter the password for mdct-integrations@coforma.io at coforma.1password.com"
send -- "$env(OP_PASSWORD)\r"
expect eof